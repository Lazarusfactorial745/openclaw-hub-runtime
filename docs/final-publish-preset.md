# Final Publish Preset

This file is the recommended final default if you want to publish the repository now without spending more time debating naming or positioning.

## Final repository name

**`openclaw-hub-runtime`**

Why this one:

- clear and searchable
- sounds like a reusable framework, not only a themed demo
- leaves room for multiple shells beyond Jarvis

## Final GitHub About

Themeable real-time AI command interfaces for OpenClaw.

## Final GitHub description

OpenClaw Hub Runtime is a modular frontend runtime for building AI command surfaces with streaming UX, reconnect-aware diagnostics, and reusable shell bindings.

## Final topics

- `openclaw`
- `ai-ui`
- `ai-terminal`
- `frontend-runtime`
- `realtime-ui`
- `streaming-ui`
- `websocket`
- `chat-interface`
- `terminal-ui`
- `developer-tools`

## Final screenshot order

Use this order on the GitHub repository page:

1. Jarvis normal runtime
2. Battle-damaged poster frame
3. Minimal Hub

## Final release title

**OpenClaw Hub Runtime 0.1.0 — First public release candidate**

## Final release notes

OpenClaw Hub Runtime 0.1.0 is the first public release candidate of a modular frontend runtime for building AI command interfaces on top of OpenClaw.

Highlights:

- flagship J.A.R.V.I.S.-style shell
- minimal second shell proving portability
- reusable runtime modules
- OpenClaw Gateway adapter and protocol normalizer
- reconnect, recovery, and diagnostics foundations
- deterministic resume test harness
- architecture, theming, adapter, onboarding, contribution, changelog, and launch docs

This release should be understood as the first strong public reference implementation of the runtime direction.

## Final launch post (English)

I’m open-sourcing **OpenClaw Hub Runtime**.

It’s a modular frontend runtime for building battle-ready AI terminals on top of OpenClaw, with:

- a flagship J.A.R.V.I.S.-style shell
- a minimal second shell proving portability
- real OpenClaw Gateway integration
- soft-throttled streaming output
- reconnect / diagnostics / resume foundations

The goal is not just to make a pretty chat UI. The goal is to make a reusable runtime for AI command interfaces.

Repo:

`<your-repo-url>`

## Final launch post (Chinese)

我正式把 **OpenClaw Hub Runtime** 开源了。

它是一套构建在 OpenClaw 之上的 AI 命令界面前端 Runtime，目前已经包含：

- 一个 J.A.R.V.I.S. 风格旗舰壳层
- 一个极简壳层，用来证明 Runtime 可复用
- 真实 OpenClaw Gateway 接入
- 柔顺节流的流式输出
- 断线重连、诊断面板、续写恢复基础能力

这个项目的目标不是做“又一个漂亮的聊天界面”，而是做一套真正可复用的 **AI 终端前端 Runtime**。

仓库地址：

`<你的仓库地址>`

## Final recommendation

If you want to publish now, do it with this exact stack:

- repository name: `openclaw-hub-runtime`
- About: `Themeable real-time AI command interfaces for OpenClaw.`
- description: use the full description above
- screenshot order: Jarvis -> Battle-damaged -> Minimal
- first release title: `OpenClaw Hub Runtime 0.1.0 — First public release candidate`
