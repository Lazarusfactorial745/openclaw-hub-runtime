# Launch Content Pack

## Positioning

**OpenClaw Hub Runtime** is a reusable frontend runtime for building themeable real-time AI command interfaces on top of the real OpenClaw Gateway.

**Short hook:** not just another chat skin.

**Core claim:** one runtime, multiple shells, real backend integration, and resilience UX built in.

## GitHub About / Description / Topics

### About
Themeable real-time AI command interfaces for OpenClaw.

### Description
OpenClaw Hub Runtime is a modular frontend runtime for building AI command surfaces with streaming UX, reconnect-aware diagnostics, and reusable shell bindings.

### Topics
- openclaw
- ai-ui
- ai-terminal
- frontend-runtime
- realtime-ui
- streaming-ui
- websocket
- terminal-ui
- developer-tools
- command-interface

## README conversion hooks

Use these ideas repeatedly:

- real backend, not a mock demo
- runtime, not a one-off skin
- reconnect / recovery is a feature, not an afterthought
- Jarvis is the flagship shell, not the whole product
- minimal shell proves portability

## Release framing

### Release title
OpenClaw Hub Runtime v0.1.0 — First Public Reference Runtime

### Release subtitle
A battle-ready reference runtime for real-time AI command interfaces on OpenClaw.

### Release bullets
- flagship J.A.R.V.I.S.-style shell
- reusable runtime core
- real OpenClaw Gateway adapter
- soft-throttled streaming UX
- reconnect diagnostics and recovery foundations
- deterministic resume showcase
- minimal second shell proving portability

## Channel copy

### GitHub release intro
This release turns OpenClaw Hub Runtime into a public reference implementation: a reusable runtime for real-time AI command interfaces on top of the real OpenClaw Gateway.

It is not just a themed chat page. The point is to separate shell identity from runtime capability, so one runtime can power multiple AI command surfaces with streaming, reconnect diagnostics, and recovery behavior built in.

### X / Twitter
I open-sourced **OpenClaw Hub Runtime** — a reusable frontend runtime for building battle-ready AI terminals on top of the real OpenClaw Gateway.

Highlights:
- real backend integration
- streaming UX
- reconnect / recovery behavior
- Jarvis as the flagship shell
- a minimal second shell proving portability

Not just another chat skin.

### Reddit
I’ve been working on an OpenClaw frontend runtime that tries to be more than a themed chat UI.

The current result is **OpenClaw Hub Runtime**: a modular runtime with a flagship Jarvis-style shell, real Gateway integration, soft-throttled streaming, reconnect diagnostics, and a minimal second shell to prove the architecture is reusable.

I’m especially interested in feedback on the runtime direction: what would make a reusable AI command interface stack genuinely worth adopting or following?

### Hacker News
Show HN: OpenClaw Hub Runtime — a reusable real-time AI command interface runtime

This is a frontend runtime for OpenClaw rather than a one-off chat skin. The interesting parts are the runtime split, the real Gateway adapter, the reconnect / recovery UX, and the fact that the same runtime already powers both a Jarvis-style shell and a minimal shell.

### 中文社区
我把 **OpenClaw Hub Runtime** 这套前端运行时整理到了可公开发布的状态。

它不是“又一个聊天 UI 套壳”，而是一套面向 OpenClaw 的**可复用实时 AI 命令界面 Runtime**：
- 真正接 OpenClaw Gateway
- 有流式输出体验
- 有断线重连与诊断恢复能力
- 同一套 runtime 已经能跑 Jarvis 风格壳层和一个极简壳层

核心目标不是做一页酷炫界面，而是做一套可以继续演化的 runtime。

## Distribution notes

### Best first-wave proof points
1. normal Jarvis runtime screenshot
2. battle-damaged reconnect poster screenshot
3. minimal shell screenshot

### Best first-wave angle
- runtime > skin
- resilience > pure aesthetics
- real integration > mock demo

### Call to action
If this direction is interesting to you, star/watch the repo and tell me which part should be pushed next: shell portability, recovery UX, or runtime APIs.
