# Launch Kit

## Repository name ideas

- `openclaw-hub-runtime`
- `openclaw-ai-terminal`
- `battle-ready-ai-terminal`

Recommended default:

**`openclaw-hub-runtime`**

It is the clearest name if you want the repository to read as a reusable framework rather than only a Jarvis-style demo.

## GitHub About text

### Short version

Themeable real-time AI command interfaces for OpenClaw.

### Medium version

OpenClaw Hub Runtime is a modular frontend runtime for building AI command surfaces with streaming UX, reconnect-aware diagnostics, and reusable shell bindings.

### Positioning line

A runtime for battle-ready AI terminals, with Jarvis as the first reference shell.

## Recommended topics

- `openclaw`
- `ai-ui`
- `ai-terminal`
- `frontend-runtime`
- `realtime-ui`
- `streaming-ui`
- `websocket`
- `chat-interface`
- `terminal-ui`
- `developer-tools`

## Demo order

Use this order when showing the repository publicly:

1. Jarvis shell normal mode
2. Battle-damaged poster mode
3. Minimal Hub

## Screenshot order

Recommended screenshot order:

1. normal Jarvis runtime
2. battle-damaged reconnect poster
3. minimal shell

## Release checklist

- finalize repository name
- set GitHub About text
- add repository topics
- upload key screenshots
- confirm README image order
- confirm `LICENSE` is present
- confirm `CHANGELOG.md` is present
- confirm `docs/architecture.md`, `docs/theming.md`, and `docs/adapter-api.md` are linked
- verify both demo URLs load locally
- publish the first release note

## Release title

**OpenClaw Hub Runtime 0.1.0 — First public release candidate**

## Release summary

This release packages the first public reference implementation of OpenClaw Hub Runtime.

Highlights:

- flagship J.A.R.V.I.S.-style shell
- reusable runtime modules
- OpenClaw Gateway adapter
- reconnect and recovery foundations
- deterministic resume test harness
- minimal second shell proving portability
- architecture, theming, adapter, onboarding, contribution, and launch docs

## Launch post (English)

I have open-sourced **OpenClaw Hub Runtime** — a modular frontend runtime for building battle-ready AI terminals on top of OpenClaw.

It currently includes:

- a flagship J.A.R.V.I.S.-style shell
- a minimal second shell proving portability
- real OpenClaw Gateway integration
- soft-throttled streaming output
- reconnect / diagnostics / resume foundations

This is not just a themed chat UI. The real goal is a reusable runtime for AI command interfaces.

Repo:

`<your-repo-url>`

## Launch post (Chinese)

我把 **OpenClaw Hub Runtime** 开源了。

它不是单纯的一个好看的聊天界面，而是一套可以复用的 AI 终端前端 Runtime。当前版本已经包含：

- 一个 J.A.R.V.I.S. 风格旗舰壳层
- 一个极简壳层，用来证明 Runtime 可复用
- 真实 OpenClaw Gateway 接入
- 柔顺节流的流式输出
- 断线重连、诊断面板、续写恢复基础能力

目标不是做“又一个套壳聊天页”，而是做一套可主题化、可复用、带战术终端质感的 AI 命令界面 Runtime。

仓库地址：

`<你的仓库地址>`
